.section.home
  = link_to "Home", root_path, :class => active_on("title", :pages => [:homepage])

.section.home.fortunes
  = link_to "Fortunes", root_path, :class => active_on("fortune title", :fortunes => true)
  - if can? :create, Fortune
    = link_to "New", new_fortune_path, :class => "new"

-# .section.headlines
-#   = link_to "Headlines", root_path, :class => active_on("headline title", :headlines => true)
-#   - if can? :create, Headline
-#     = link_to "New", new_headline_path, :class => "new"

- Category.all.each do |category|
  .section.home
    = link_to category.title, category_path(category), :class => "title"

.section#feeds
  = link_to "Fortunes", feed_path, :class => "feed"

-# .section.writers
-#   = link_to "Writers", writers_page_path, :class => active_on("writers title", :writers => true)
